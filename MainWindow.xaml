<Window x:Name="PaginaPrincipal" x:Class="GymApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GymApp"
        mc:Ignorable="d"
        Title="Menu Principal" Height="1080" Width="1920" WindowStyle="ThreeDBorderWindow" WindowStartupLocation="CenterScreen" WindowState="Maximized" ResizeMode="NoResize">
    <Grid Background="#FFB3B3B3">
        <!-- Define las columnas para el menú lateral y el contenido principal // Height="621" Width="1179" -->

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <!-- Ancho automático para el menú lateral -->
            <ColumnDefinition Width="Auto"/>
            <!-- El resto del ancho para el contenido principal -->
        </Grid.ColumnDefinitions>

        <!-- Menú lateral -->
        <Border Padding="0,180,0,0" Background="#FF4A4747">
            <StackPanel VerticalAlignment="Top">
                <Separator Margin="45"/>
                
                <!-- Opción Inscripciones -->
                <Button x:Name="BtnInscripciones" Content="Inscripciones" Margin="5" FontSize="25" Click="BtnInscripciones_Click"/>
                <Separator Margin="45"/>

                <!-- Opción Clientes -->
                <Button x:Name="BtnClientes" Content="Clientes" Margin="5" FontSize="25"/>
                <Separator Margin="45"/>

                <!-- Opción Rutinas -->
                <Button x:Name="BtnRutinas" Content="Rutinas" Margin="5" FontSize="25"/>
                <Separator Margin="45"/>
                
            </StackPanel>
        </Border>

        <!-- Contenido principal -->
        <Border Grid.Column="1" Background="#FF312C2C">
            <Frame x:Name="frameInscripciones" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Width="1763px"/>
        </Border>
    </Grid>
</Window>
